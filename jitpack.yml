jdk:
  - openjdk21
before_install:
  - sdk install java 21.0.2-open
  - sdk use java 21.0.2-open
install:
  - ./gradlew :spice-core:publishToMavenLocal -x test
  - ./gradlew :spice-springboot:publishToMavenLocal -x test
  - ./gradlew :spice-extensions-sparql:publishToMavenLocal -x test
  - ./gradlew :spice-eventsourcing:publishToMavenLocal -x test
